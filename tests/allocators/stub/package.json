{
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm run build:untouched && npm run build:optimized",
    "build:untouched": "node ../../../bin/asc assembly/index.ts -t untouched.wat -o untouched.wasm --runtime stub --sourceMap --stats",
    "build:optimized": "node ../../../bin/asc assembly/index.ts -t optimized.wat -o optimized.wasm --runtime stub --sourceMap --stats --noAssert --optimize"
  }
}
