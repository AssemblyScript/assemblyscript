{
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm run build:untouched && npm run build:optimized",
    "build:untouched": "node ../../../bin/asc assembly/index.ts -t untouched.wat -o untouched.wasm --sourceMap --stats --debug",
    "build:optimized": "node ../../../bin/asc assembly/index.ts -t optimized.wat -o optimized.wasm --sourceMap --stats --noAssert --optimize"
  }
}
