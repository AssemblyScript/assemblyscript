{
  "name": "b",
  "description": "simple package with one dependency",
  "scripts": {
    "build": "../../node_modules/.bin/asc assembly/index.ts --noEmit --runtime stub && ../../node_modules/.bin/asc assembly/index.ts --noEmit --runtime stub --listFiles"
  },
  "types": "assembly/index.ts",
  "author": "Willem Wyndham",
  "dependencies": {
    "a": "file:../a"
  }
}
