{
  "name": "d",
  "description": "package with two dependency and two transitive dependencies",
  "scripts": {
    "build": "asc assembly/index.ts --path node_modules --noEmit --runtime stub --validate --traceResolution"
  },
  "types": "assembly/index.ts",
  "author": "Willem Wyndham",
  "dependencies": {
    "as": "file:../as",
    "c": "file:../c"
  }
}
